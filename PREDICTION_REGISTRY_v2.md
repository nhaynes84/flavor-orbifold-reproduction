
---
**ANNOTATION 2026-07-08 (additive; registry body unchanged):** New calc
bet from M6 session 2 — the crease-fee formula 1/α_i = (8π/3)ℓ_i +
(ℓ_i+2)c₀² demands **α_s(M_Z) = 0.11769** (current 0.1179 ± 0.0009,
−0.2σ). FCC-ee-class precision decides. Search cost at registration:
8 zero-parameter expressions scanned (m6_session2.py); the bet, not the
fit, is the evidence. Same measurement also resolves the round-3
crossing-separation question and the ε₀ = c₀² vs 1/(8π) degeneracy.

**CORRECTION 2026-07-08 (same day):** the annotation above overclaimed
— the α_s measurement tests the crease-fee formula but CANNOT resolve
ε₀ = c₀² vs 1/(8π) (they differ by 0.0075% in α_s, ~12× below FCC-ee
resolution; see 40-EPSILON-THRASH.md). Two verdicts per experiment,
not three.

**PROTOCOL EXTENSION 2026-07-09 (θ₂₃ scenario doors, pre-committed):**
Door 3 — EXACT MAXIMAL (sin²θ₂₃ = 0.500 ± tight): this KILLS the
prediction same as lower octant (0.548 excluded from both sides; the
corridor is 0.5 < s² < 0.555). Stated loudly: "no one is wrong, it's
maximal" = our loss, and μ–τ reflection symmetry's win. No lawyering.
Door 4 — PERSISTENT NON-RESOLUTION (DUNE/HK disagree beyond combined
systematics): this is new physics (NSI/sterile/decoherence class), NOT
a vindication of "contextual parameters." Pre-commitment: the ONLY
legitimate framework response is a NEW dated prediction — derive the
sign+size of the atmospheric-vs-accelerator split from decoherence
depth BEFORE improved fits, or stay silent. Contextuality-as-excuse is
banned; contextuality-as-predicted-number is the only door-4 play.

**PRE-REGISTRATION REFINEMENT 2026-07-09 (θ₂₃ two-tier structure —
filed BEFORE resolution, from derivation structure, not after data):**
The formula's two components have different pedigrees: the MAGNITUDE of
deviation from maximal, (2+√3)δx = 0.048, rides the sturdy chain; the
SIGN (octant) rides the shift-direction convention chain. Registered
tiers: PRIMARY = sin²θ₂₃ = 0.548 (unchanged; full win / full death).
SUBORDINATE (folded) = |sin²θ₂₃ − 1/2| = 0.048: a resolution at the
mirror value 0.452 kills the octant claim and sustains the folded claim
— a defined partial-demotion tier. Physics context: the leading
observable sin²(2θ₂₃) is octant-blind (two-to-one; the war = mirror-
minima hopping), so the folded quantity is what the waveform most
directly constrains — the octant is preimage/cover data. λ-note: the
camps' deviations (0.030, 0.061) straddle 0.048 — observation only,
NOT evidence. Exact-maximal (Door 3) still kills BOTH tiers.
